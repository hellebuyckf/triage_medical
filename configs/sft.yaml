training:
  max_seq_length: 1024
  learning_rate: 0.0002
  epochs: 5
  batch_size: 4
  grad_accum: 4
  seed: 42

lora:
  r: 32
  alpha: 64
  dropout: 0.05
  target_modules:
    - q_proj
    - v_proj
    - k_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
